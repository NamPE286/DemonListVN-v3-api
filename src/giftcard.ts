import express from 'express'

const router = express.Router()

router.route('/:code')
    .get(async (res, req) => {
        // TODO
    })

router.route('/:code')
    .patch(async (res, req) => {
        // TODO
    })

export default router